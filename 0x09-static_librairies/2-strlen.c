#include "main.h"
/**
 * _strlen -function to find a length of a char
 *
 * @s: pointer of char
 *
 * Return: the length of a character
 */

int _strlen(char *s)
{
	int length;

	length = strlen(s);
	return (length);
}
